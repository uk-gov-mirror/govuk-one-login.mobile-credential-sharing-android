name: Release mobile-credential-sharing-android

on:
  push:
    branches:
      - main

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  GITHUB_ACTOR: ${{ github.actor }}
  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

jobs:
  linting:
    name: "Lint code base"
    uses: ./.github/workflows/linting.yml
    secrets: inherit


  bump-version:
    name: "Bump semantic version"
    # Requires 'linting' to pass as linting verifies conventional commit structure.
    needs: linting
    permissions:
      contents: write
      id-token: write
      packages: read
    secrets: inherit
    uses: ./.github/workflows/bump-version.yml
    with:
      # Empty to denote a 'production' release.
      pre-release-type: ''


  dependency-graph:
    name: "Submit gradle dependency graph"
    permissions:
      contents: write
      id-token: write
      packages: read
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          lfs: 'true'
          submodules: 'true'
          fetch-depth: 0

      - name: Setup
        uses: ./.github/actions/android-setup

      - name: Generate and submit dependency graph
        uses: gradle/actions/dependency-submission@9c971963bec38e04b3d30dcc455b5382be2fdbfb # v6.3.0


  publishing:
    name: "Publish code base"
    needs: bump-version
    if: ${{ needs.bump-version.outputs.current-version != needs.bump-version.outputs.new-version }}
    permissions:
      contents: write
      id-token: write
      packages: write
    secrets: inherit
    uses: ./.github/workflows/publishing.yml
    with:
      ref: ${{ needs.bump-version.outputs.new-version }}
      version-code: ${{ needs.bump-version.outputs.version-code }}


  testing:
    name: "Test code base"
    uses: ./.github/workflows/testing.yml
    secrets: inherit
